feat: add custom titlebar, adjustable result duration and softer draw animation

- 移除首页副标题「让每一次课堂点名都公平、清晰、轻松。」
- 抽取结果全屏停留时长默认 3 秒并可在设置中调整（1500-10000 毫秒），旧存档的 1000 毫秒在加载时迁移为新默认值
- 窗口改为 frame: false，标题栏由渲染器按主题自绘（最小化、最大化/还原、关闭），新增窗口控制与最大化状态两个 IPC 通道
- 设置抽屉的「学生权重」与「最近抽取」默认折叠，折叠时内部控件不渲染
- 抽取动画改为二次缓出加随机抖动，名字切换节奏不均匀且避免连续同名
- 修复抽屉关闭按钮复用整宽按钮样式导致标题被挤成竖排，以及固定标题栏压住抽屉顶部的问题
