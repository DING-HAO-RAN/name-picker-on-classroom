# 2026-09-15

## 本轮改动（抽卡视觉三项 + 升星开关）

1. **星星金色化**：STAR_COLORS 删除，`.card-draw__star` 统一 #ffd54a 金色 + 四向白色 text-shadow 模拟细白描边 + 金色 glow。
2. **卡背与卡面同色**：back 元素挂 STAR_THEME_CLASSES；CSS 新增 `.card-draw__face--back.card-draw__face--star1..5` 深色渐变变体（同色系加深）+ 同色描边；背面加入 band/名字（card-draw__back-name 大字体白字强投影，覆盖正面按星变色的文字规则）/星星行，保留小星徽与流光。翻面前即可辨认稀有度。
3. **自动升星开关**：settings.autoStarUpgrade（默认 true，normalize boolean）；handleDraw 的 bumpStar 依 currentRoster.settings.autoStarUpgrade 决定是否升星（计数仍累计）；设置 UI 放在「按星级抽取」分组内（PillSwitch）。
- 测试：tsc 0 错；vitest 182 全过（CardDrawOverlay 测试更新：正反两面各一组星星、star 类 ×2）；App.test「Ctrl+O 导入」waitFor 加 5s 超时治全量偶发超时。
- 打包 release16（Setup 84.7MB + portable 84.5MB），包内验证 back-name/autoStarUpgrade。下轮输出目录 release17。

## 本轮改动（卡背纯色化、翻面优化、发布 v1.1.0）

1. **卡背翻面前只显示颜色**：背面去掉名字/星星（back-name CSS 一并删除），只留星级渐变 + 色带 + 流光 + ✦ 装饰。注意 DOM 断言要限定 `.card-draw__face--back` 内（正面在 DOM 始终存在，只是 3D 遮挡）。
2. **翻面动画优化**：cardFlipReveal 改单程旋转（0→55% 185° 过冲→78% 179°→100% 180° 定格，640ms），删除中段 92° 折返（顿挫来源）。
3. **发布 v1.1.0**：package.json 升 1.1.0；push master→main；Release https://github.com/DING-HAO-RAN/name-picker-on-classroom/releases/tag/v1.1.0（两附件上传成功，附件名空格已 PATCH 修正）。
- **重要教训**：
  - `git add -A` 前 .gitignore 必须先包含当轮输出目录（release15/16 曾被误提交，192MB unpacked exe 超过 GitHub 100MB 限制导致 push 被拒）。**提交后必须核对 `git diff --cached --name-only`**。
  - 本沙箱 git push 走 credential-manager 会静默 exit 128：解法 = `-c credential.helper=` 禁用助手 + URL 嵌 x-access-token 令牌 + **dangerouslyDisableSandbox**（沙箱内网络写被拦截）。
  - git rm --cached / add -A 对已跟踪文件不适用 ignore；正确顺序 = reset --soft → `git reset`（unstage 全部）→ add -A → commit。
  - GITHUB_TOKEN 由用户提供（会话内一次性使用，不落盘）。
  - 下轮输出目录 release19；.gitignore 需提前加 release19/。

## 本轮改动（删除悬浮球 + 抽卡按钮溢出修复）

1. **悬浮球整体删除**：FloatingBall.tsx 删除；renderer/main.tsx 移除 ?window=floating 分支；main/index.ts 删 floatingWindow/createFloatingWindow/hideMainWindowToFloating/floatingControls/isQuitting/loadWindowBehaviorSettings；ipcHandlers/preload/shared 同步删 floatingControl 通道与 closeAction/showFloatingBall 设置（normalize 白名单自动丢弃旧档字段）；设置抽屉「窗口与启动」只留开机自启。**关闭窗口即退出程序**。
2. **收下结果按钮溢出修复**：.card-draw 改 flex-start + 标题/按钮 shrink 0；.card-draw__stage flex:1 + overflow-y:auto + align-content: safe center——大数量卡牌舞台内部滚动，按钮永远在视口底部。
3. **测试**：删悬浮球相关 3 个用例；App.test「Ctrl+O 导入」改为 act+flushPromises 确定性断言（根治 waitFor 轮询 flaky）。tsc 0 错；180 用例全过。
4. 打包 release19（Setup/portable 84.7/84.5MB），包内验证 floatingControl/floating-ball/showFloatingBall 均已消失、cardFlipReveal 在。下轮输出目录 release20（.gitignore 已含到 release18，需补 19/20）。
- 提醒：git commit 前必须 `git diff --cached --name-only | grep ^release` 核对（commit-r19-leak 机制）。


